export { COMPARE_CONFIG, type CompareConfig } from "@/lib/compare/config";
export {
  lerp,
  clamp01,
  easeInOut,
  easeOutCubic,
  cardSlideProgress,
  activeCardIndex,
  cardCollapseProgress,
  columnProgress,
  columnCardSlideProgress,
  compareScrollPhases,
  compareScrollMetrics,
  sectionAnimationProgress,
  sectionReleaseProgress,
  sectionReleaseTranslateT,
  syncedStepCount,
  sectionScrollHeight,
} from "@/lib/compare/progress";
export { useCompareScroll } from "@/lib/compare/useCompareScroll";
export { useComparePin } from "@/lib/compare/useComparePin";
